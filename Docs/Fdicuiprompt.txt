FDIC 370 — the gap between the tab strip and the page H1 is STILL there
(~60px, identical on all 8 pages). Previous attempts adjusted .wf-page
padding and did not fix it, which means the space is coming from something
else. Do not guess again.

=== STEP 1: DIAGNOSE, CHANGE NOTHING ===
Open _Layout.cshtml and list, in document order, EVERY element between the
closing tag of the tab strip <nav>/<ul> and the @RenderBody() call. Include
empty divs, <br>, leftover breadcrumb wrappers, Bootstrap .row/.container
wrappers, <main>, <hr>, and anything with a class you don't recognise.
For each one, state its computed height, margin-top, margin-bottom, padding.

Then do the same for the first elements INSIDE the rendered page, from the
top of RenderBody down to the H1.

Report this as a list before making any change. State explicitly which
element or elements account for the ~60px.

Likely culprits to check specifically:
  - an empty <div class="breadcrumb..."> left behind when the breadcrumb
    content was removed — the wrapper still has padding
  - <main role="main" class="pb-3"> or similar Bootstrap utility on the
    layout's main element
  - .container / .row default padding stacking with .wf-page padding
  - margin collapse: the H1's margin-top escaping its parent because the
    parent has no border or padding
  - a min-height or fixed height on the tab strip container taller than
    the tabs themselves

=== STEP 2: FIX ONLY WHAT YOU IDENTIFIED ===
Remove or zero the specific offenders. If it's an empty wrapper, delete the
markup. If it's margin collapse, set the page container's padding-top and
h1 { margin-top: 0 } together.
Do not add negative margins to compensate — find the actual source.

Target: exactly 16px between the bottom edge of the tab strip's bottom
border and the cap height of the H1 text.

=== STEP 3: VERIFY ===
Report the measured gap on Upload, SQL Editor and ConsolidatedOutput.
All three must be 16px.
