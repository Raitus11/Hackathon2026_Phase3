Context: ASP.NET Core 8 Razor Pages app (FDIC370WebApplication). Wells Fargo theme.
Stylesheet: wwwroot/css/site.css. Grid pages under /Pages:
ControlFailureRecords.cshtml, ControlFailureRecords_trino.cshtml,
ControlFailureDetail.cshtml, ControlFailureDetail_trino.cshtml,
ConsolidatedOutput.cshtml, ConsolidatedOutput_Trino.cshtml.

Task: Refine the existing `.grid-link` drill-down styling. Current state is too loud —
every drillable cell renders red at rest, the CSS tooltip is clipped by the table
wrapper, and the header hint glyph is mojibaked.

Changes required:

1. Rewrite the `.grid-link` rule in site.css:
   - Rest state: color #2b2b2b, 1px dotted #bfbfbf bottom border, font-weight 500.
   - Hover / :focus-visible: color #D71E28, 1px solid #D71E28 bottom border.
   - Chevron "\203A" via ::after, position: absolute; right: .15rem; opacity 0 -> 1
     on hover/focus. Give the anchor padding-right: .75rem so the chevron never
     causes reflow.
   - transition color and border-color at .12s ease.
   - `.grid-link:visited` must stay #2b2b2b (no purple).
   - Keep the existing :focus-visible outline (2px #D71E28, offset 2px).

2. Remove the CSS tooltip entirely: delete the `.grid-link[data-tip]:hover::before`
   rule and strip every `data-tip` attribute from the .cshtml files. Keep the
   `aria-label` on each anchor. Do not reintroduce the native `title` attribute.

3. Fix the drillable-column header hint: replace the corrupted glyph with the HTML
   entity `&#8627;` inside `<span class="drill-hint" aria-hidden="true">`. Verify all
   affected .cshtml files are saved as UTF-8 with BOM.

4. Reduce link density: apply `.grid-link` to Control ID and Check Reference only on
   the ControlFailureRecords / ControlFailureDetail pages (both Trino and non-Trino).
   Unique Cust ID stays a drill link only on the ConsolidatedOutput pages. Remove
   `.grid-link` from Unique Cust ID on the control-failure grids and render it as
   plain text there.

Constraints:
- Preserve every existing href, route value and query string exactly.
- No new packages, no JavaScript, no Bootstrap version change.
- Keep Trino and non-Trino page variants byte-identical in markup shape.
- Output minimal find-and-replace diffs per file, not full file rewrites.
