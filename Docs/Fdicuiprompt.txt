Context: ASP.NET Core 8 Razor Pages app (FDIC370WebApplication). Pages under /Pages:
ControlFailureRecords.cshtml, ControlFailureRecords_trino.cshtml,
ControlFailureDetail.cshtml, ControlFailureDetail_trino.cshtml,
ConsolidatedOutput.cshtml, ConsolidatedOutput_Trino.cshtml.
Global stylesheet: wwwroot/css/site.css. Wells Fargo theme (red #D71E28, gold accent).

Task: Drill-down cells in the data grids are currently plain <a> tags with a `title`
attribute — they look like static text and give no visual affordance until hover, and
the native tooltip is slow and unthemable. Make drillable cells clearly clickable.

Requirements:
1. Add a reusable `.grid-link` CSS class in site.css:
   - color #D71E28, hover #A31219
   - 1px dotted bottom border at rest, solid on hover/focus
   - a "›" chevron via ::after that fades and slides in on hover/focus only
   - :focus-visible outline 2px #D71E28, offset 2px (keyboard accessible)
2. Replace the native tooltip with a CSS tooltip driven by a `data-tip` attribute
   (::before, positioned above the link, dark background, pointer-events: none,
   z-index above table rows). Keep an aria-label on the anchor for screen readers.
3. Add `.table-drill tbody tr:hover { background: #FFF7F7; }` and apply the
   `table-drill` class to the grid tables.
4. Add a subtle `↳` hint span (aria-hidden) in the <th> of drillable columns only.
5. Apply `.grid-link` ONLY to columns that actually navigate:
   - Control ID  -> ControlFailureDetail / ControlFailureDetail_trino
   - Check Reference -> ControlFailureCheckDetail / _trino
   - Unique Cust ID -> ConsolidatedOutputDetail / _Trino
   Leave SOR ID, LOB Name, Unique Acct ID and all other cells unstyled.
6. Preserve every existing href, route value and query string exactly. Do not change
   page handlers, models, or data binding.
7. Keep the Trino and non-Trino page variants consistent — same classes, same markup shape.

Constraints:
- No new NuGet or npm packages, no Bootstrap version change, no JavaScript.
- Output as minimal find-and-replace diffs per file, not full file rewrites.
- Verify no ancestor container has overflow:hidden that would clip the CSS tooltip;
  if one does, flag it instead of silently changing the layout.
