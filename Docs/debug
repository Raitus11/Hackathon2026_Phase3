python -c "
from pci_sentinel.orchestrator import start_run
import glob
files=[(p.split('/')[-1], open(p,encoding='utf-8-sig').read()) for p in sorted(glob.glob('../sample_data/DS*.csv'))]
r=start_run(files, thread_id='t')['result']
print('source_exposure rows:', len(r.plan.get('source_exposure',{}).get('per_source',[])))
"
